#include <bits/stdc++.h>
using namespace std;

int main() {
    int f, r;
    
    while(cin >> f >> r) {
        int F[f], R[r];

        for(int i=0; i<f; ++i)
            cin >> F[i];

        for(int i=0; i<r; ++i)
            cin >> R[i];

        double ans = 0;
        for(int i=0; i<f; ++i) {
            for(int j=i+1; j<r; ++j) {
                R[j] / 
            }
        }
    
    }
    return 0;
}